import styled from 'styled-components';

export const _popover = styled.div`
  ${({ theme }) => theme.styles.popover.popover}
`;

export const _container = styled.div`
  ${({ theme }) => theme.styles.popover.popover_container}
`;
